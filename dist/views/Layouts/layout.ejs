<html lang="en">
    <%- include('../partials/head') %>


    <body>
<main>
    <%- body %>
    </main>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js
    "></script>
    <script src="https://kit.fontawesome.com/b96ae1bf40.js" crossorigin="anonymous"></script>
    <!-- MDB -->
<script
type="text/javascript"
src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.js"
></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>

<script>

  var currentUrl = window.location.pathname;

  var navLinks = document.querySelectorAll('.nav-link');

  navLinks.forEach(function (link) {
    if (link.getAttribute("href") === currentUrl) {
      link.classList.add('active');
    } else {
      link.classList.remove('active');
    }
  });

  var coursLink = document.querySelector('.navbar-nav .dropdown a[href^="/cours"]');
  var dropdownItems = document.querySelectorAll('.dropdown-item');

  dropdownItems.forEach(function (item) {
    if (item.getAttribute("href") === currentUrl) {
      coursLink.classList.add('active');
      item.classList.add('active');
    } else {
      item.classList.remove('active');
    }
  });

    // Function to be called when the card enters the viewport
    function handleIntersection(entries, observer) {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          // Add the "fade-in" class to trigger the animation
          entry.target.classList.add('fade-in');
          // Unobserve the card to prevent duplicate animations
          observer.unobserve(entry.target);
        }
      });
    }
  
    // Create an Intersection Observer
    const observer = new IntersectionObserver(handleIntersection, {
      root: null, 
      threshold: 0.2, 
    });
  
    // Observe all cards with the "fade-hidden" class
    const cards = document.querySelectorAll('.fade-hidden');
    cards.forEach((card) => {
      observer.observe(card);
    });

//modal payement
 
const openModalButtons = document.querySelectorAll('[id^="openmodal"]');

const modal = document.getElementById("myModal");

openModalButtons.forEach(function (button) {
  button.addEventListener("click", function () {
    modal.style.display = "block"; // Affichez le modal

  });
});

// Gestionnaire d'événement pour le bouton de fermeture du modal
const closeModalBtn = document.getElementById("closeModalBtn");
closeModalBtn.addEventListener("click", function () {
  modal.style.display = "none"; // Masquez le modal

});

  // Lorsque l'utilisateur clique en dehors du modal, cachez le modal
  window.onclick = function(event) {
      if (event.target == modal) {
          modal.style.display = "none";
      }
    }
  	
    



// Sélectionnez tous les éléments avec la classe 'radio' dans la classe 'radio-group'
const radioElements = document.querySelectorAll('.radio-group .radio');

// Ajoutez un gestionnaire d'événements 'click' à chaque élément 'radio'
radioElements.forEach(function(radio) {
    radio.addEventListener('click', function() {
        // Supprimez la classe 'selected' de tous les éléments 'radio' dans la classe 'radio-group'
        radioElements.forEach(function(radioElement) {
            radioElement.classList.remove('selected');
        });

        // Ajoutez la classe 'selected' à l'élément 'radio' actuel
        this.classList.add('selected');
    });
});







      
//modal login


const openModallogin = document.querySelectorAll('[id^="myloginmodal"]');

const login = document.getElementById("loginmodal");

openModallogin.forEach(function (button) {
  button.addEventListener("click", function () {
    login.style.display = "block"; // Affichez le modal

  });
});

// Gestionnaire d'événement pour le bouton de fermeture du modal
const closeModallogin = document.getElementById("closeModallogin");
closeModallogin.addEventListener("click", function () {
  login.style.display = "none"; // Masquez le modal

});

  // Lorsque l'utilisateur clique en dehors du modal, cachez le modal
  window.onclick = function(event) {
      if (event.target == login) {
          login.style.display = "none";
      }
    }
  	



//modal register


const openModalregister = document.querySelectorAll('[id^="myregistermodal"]');

const register = document.getElementById("registermodal");

openModalregister.forEach(function (button) {
  button.addEventListener("click", function () {
    register.style.display = "block"; // Affichez le modal

  });
});

// Gestionnaire d'événement pour le bouton de fermeture du modal
const closeModalregister = document.getElementById("closeModalregister");
closeModalregister.addEventListener("click", function () {
  register.style.display = "none"; // Masquez le modal

});

  // Lorsque l'utilisateur clique en dehors du modal, cachez le modal
  window.onclick = function(event) {
      if (event.target == register) {
          register.style.display = "none";
      }
    }
  	

    


  	

  </script> 
  

    </body>
</html>