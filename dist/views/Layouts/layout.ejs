<html lang="en">
    <%- include('../partials/head') %>


    <body>
<main>
    <%- body %>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js
    "></script>
    <script src="https://kit.fontawesome.com/b96ae1bf40.js" crossorigin="anonymous"></script>
    <!-- MDB -->
<script
type="text/javascript"
src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.js"
></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>

<script>
  // Get the current URL
  var currentUrl = window.location.pathname;
  var coursLink = document.querySelector('.navbar-nav .dropdown a[href^="/cours"]');
  var coursDropdown = document.querySelector('.navbar-nav .dropdown-item');
  
  var navLinks =document.querySelectorAll('.nav-link ');

  if (currentUrl.startsWith("/cours")) {
    coursLink.classList.add('active');
    coursDropdown.classList.add('active');
  }
  else{

  navLinks.forEach(function (link) {
  if (link.pathname === currentUrl) {
    link.classList.add('active');
  } else {
    link.classList.remove('active');
  }
  })
}
  </script>
<script>
    // Function to be called when the card enters the viewport
    function handleIntersection(entries, observer) {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          // Add the "fade-in" class to trigger the animation
          entry.target.classList.add('fade-in');
          // Unobserve the card to prevent duplicate animations
          observer.unobserve(entry.target);
        }
      });
    }
  
    // Create an Intersection Observer
    const observer = new IntersectionObserver(handleIntersection, {
      root: null, // Use the viewport as the root
      threshold: 0.2, // When 20% of the card is visible, trigger the animation
    });
  
    // Observe all cards with the "fade-hidden" class
    const cards = document.querySelectorAll('.fade-hidden');
    cards.forEach((card) => {
      observer.observe(card);
    });












  </script>
  
    
    </body>
</html>